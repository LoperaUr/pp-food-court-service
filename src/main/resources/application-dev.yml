server:
  port: 8080
spring:
  application:
    name: food-court-service

  datasource:
    url: jdbc:mysql://localhost:3306/food_court
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true